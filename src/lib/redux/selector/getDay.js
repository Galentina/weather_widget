export const getDay = (state) => {
    return state.days.days;
};

export const getChosenDays = (state) => {
    return state.days.chosenDays;
};

export const getFilter = (state) => {
    return state.days.filterValues;
};

export const getFilterLabel = (state) => {
    return state.days.filter;
};

